<div class="row" >
	<div class="col-md-4">
		<h3>CouchInn!!!</h3> 
	</div>
	<div class="col-md-2"><br>
		<% if current_user %> 
			<%= button_to 'Logout', destroy_user_session_path, method: :delete %> 
		<% else %>
    		<%= link_to 'Login', new_user_session_path %>
		<% end %>
	</div>

	<div class="col-md-2"><br>
		<%= link_to 'Lista de Couchs', couchs_path %>
	</div>

	<div class="col-md-2"><br>
		<% if current_user %> 
		<% if can? :manage, :all %>
			<%= link_to 'Lista de Usuarios', users_path %> 		
		<% end %>
		<% end %>
	</div>

	<div class="col-md-2"><br>
		<% if current_user %> 
		<% if can? :manage, :all %>
			<%= link_to 'Agregar tipo de couch', "couch_type" %>  
		<% end %>
		<% end %>
	</div>
</div><br><br>

<% if can? :read,  @couch_type %>
<table class="table">
    <tr>
    	<td colspan="2"><h3> Listado tipos de Couchs </h3></td>
    	<td colspan="2"> <%= link_to "Agregar", new_couch_type_path() , class: 'btn btn-success btn-sm' %> 
</td>
    </tr>
	<tr>
		<th> Título </th>
		<th> Descripcion </th>
		<th> Estado </th>
		<th> Opciones </th>
	</tr>

	<% @couch_types.each do |couch_type| %>
		<tr>
			<td> <%= link_to couch_type.titulo, couch_type %> </td>
			<td> <%= couch_type.descripcion %> </td>
			<td> <%= (couch_type.enabled) ? "Habilitado" : "No Habilitado"  %> </td>
			<td>
				 <%= link_to "Editar", edit_couch_type_path(couch_type), class: 'btn btn-info btn-sm' %> 
				<%= link_to "Eliminar", couch_type, method: :delete, class: 'btn btn-danger btn-sm' %>
			</td>
		</tr>
	<% end %>
</table>

<% else %>
	no tiene los permisos suficientes para admin
<% end %>